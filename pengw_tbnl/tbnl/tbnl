# Script to start "tbnl" on the device, which has a very rudimentary
# shell.
#
base=/system
export CLASSPATH=$base/framework/tbnl.jar
exec app_process $base/bin info.voidstar.android.tbnl.Tbnl "$@" 
