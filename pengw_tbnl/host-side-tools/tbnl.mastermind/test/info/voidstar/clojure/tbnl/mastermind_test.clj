(ns info.voidstar.clojure.tbnl.mastermind-test
  (:require [clojure.test :refer :all]
            [info.voidstar.clojure.tbnl.mastermind :refer :all]))

(deftest a-test
  (testing "FIXME, I fail."
    (is (= 0 1))))
